<Window x:Class="SistemaVentas.GUI.Dialogs.AbrirCajaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Abrir Caja" Height="200" Width="350"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgPaneles}"
        Foreground="{DynamicResource TextoClaro}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" Text="Ingrese el Monto Inicial de Caja" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Label -->
        <TextBlock Grid.Row="1" Text="Monto (en $):" Margin="0,0,0,5"/>

        <!-- TextBox para el monto -->
        <TextBox Grid.Row="2" x:Name="txtMontoCaja" Height="40" 
                 Style="{StaticResource InputTexto}" 
                 Text="0" Foreground="{DynamicResource TextoClaro}"/>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnAceptar" Content="Aceptar" Style="{StaticResource BotonPrimario}" 
                    Width="100" Height="40" Click="btnAceptar_Click" Margin="0,0,10,0"/>
            <Button x:Name="btnCancelar" Content="Cancelar" Style="{StaticResource BotonPrimario}" 
                    Width="100" Height="40" Click="btnCancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
